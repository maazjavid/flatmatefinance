# Deprecated: use the repository root `Dockerfile` instead.
# This app is a single Next.js server (UI + /api/*). Split backend/frontend images
# are not used for AWS (Option A — one ECS service).
#
#   docker build -f Dockerfile -t flatmate-finance .
