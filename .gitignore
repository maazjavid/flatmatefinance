node_modules
.next
out
build
dist

# Keep environment variables out of version control
.env
.env*.local

/generated/prisma

# Local SQLite DB (lives at repo root in dev; never commit it)
*.db
*.db-journal

*.tsbuildinfo
