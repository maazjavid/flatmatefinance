node_modules
.next
out
build
dist

# Keep environment variables out of version control
.env
.env*.local

# Yarn is the lockfile for this repo (npm users: do not commit package-lock.json)
package-lock.json

/generated/prisma

# Local SQLite DB (lives at repo root in dev; never commit it)
*.db
*.db-journal

*.tsbuildinfo
